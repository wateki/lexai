# Legal KB processor worker — full pipeline
supabase>=2.0.0
openai>=1.0.0
docling>=2.0.0
eyecite>=2.0.0
graphiti-core[falkordb]>=0.19.0

# PageIndex: local repo at ../../pageIndex/PageIndex (path added at runtime; no PyPI package)
